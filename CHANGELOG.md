# Changelog 

## v.0.3.0
- Added support for dynamic Pix.
- Added QRCode support for PNG, BASE64, SVG and Unicode.
- All public APIs have a Javadoc.
- Developed `PixParser` class with tree-parsing.
- Can now a BRCode text payload into a typed `PixPayload` object.
- Added `Pix` class entrypoint
