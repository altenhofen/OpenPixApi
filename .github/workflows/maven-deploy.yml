name: Maven Deploy

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Import GPG key
        uses: crazy-max/ghaction-import-gpg@v6
        with:
          gpg_private_key: ${{ secrets.GPG_PRIVATE_KEY }}
          passphrase: ${{ secrets.GPG_PASSPHRASE }}

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven

      - uses: s4u/maven-settings-action@v3.0.0
        with:
          servers: |
            [
              {
                "id": "central",
                "username": "${{ secrets.OSSRH_USERNAME }}",
                "password": "${{ secrets.OSSRH_TOKEN }}"
              },
              {
                "id": "ossrh-snapshots",
                "username": "${{ secrets.OSSRH_USERNAME }}",
                "password": "${{ secrets.OSSRH_TOKEN }}"
              }
            ]

      # DEPLOY STEP: No version manipulation needed. The tag IS the version.
      - name: Deploy to Maven Central
        run: mvn -B deploy -P maven-publisher -DskipTests
        env:
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false

      # Now we bump to the next snapshot for development
      - name: Bump next snapshot version
        run: mvn versions:set -DnextSnapshot -DprocessAllModules=true -DgenerateBackupPoms=false

      - name: Commit snapshot version
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pom.xml */pom.xml
          git commit -m "[CI] Bumped to next snapshot"
          git push origin HEAD:master
